/* Varna Towers — legacy global UI overrides retired.
   The profile now uses the universal Navigator client UI and analytical modules.
   Varna Towers-specific data remains available through its client dataset only. */
(function(){
  'use strict';
  window.__BLIS_VARNA_TOWERS_LEGACY_UI_RETIRED = true;
})();
