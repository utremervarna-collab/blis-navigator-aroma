/* BLIS Navigator — make every Overview metric/chart obey 7/30/90 period */
(function(){
  const num=v=>{const n=Number(v);return Number.isFinite(n)?n:null};
  const fmt=v=>v==null?'—':Number(v).toLocaleString('bg-BG',{maximumFractionDigits:1});
  const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const avg=a=>a.length?a.reduce((x,y)=>x+y,0)/a.length:null;
  const hist=k=>window.BLISPeriod?BLISPeriod.history(k):[];
  const marketHist=()=>{if(!window.BLISPeriod)return[];return BLISPeriod.snapshots().map(s=>{const p=s.payload||{},idx=p.indices||[];const get=k=>{const x=idx.find(i=>i.key===k);return x?num(x.value):null};const c=get('content'),pr=get('presence'),q=get('competitive');return[c,pr,q].some(v=>v==null)?null:Math.round((c*.40+pr*.25+q*.35)*10)/10}).filter(v=>v!=null)};
  const values=k=>k==='market'?marketHist():hist(k);
  function spark(vals,color){if(vals.length<2)return '<div class="ov-no-data">Нужно е още едно измерване в избрания период.</div>';const w=210,h=42,p=3,min=Math.min(...vals),max=Math.max(...vals),span=max-min||1,pts=vals.map((v,i)=>`${p+i*(w-2*p)/(vals.length-1)},${h-p-(v-min)*(h-2*p)/span}`).join(' ');return `<svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none"><polyline points="${pts}" fill="none" stroke="${color}" stroke-width="2.4" vector-effect="non-scaling-stroke"/></svg>`}
  function patchKpis(){const colors=['#2674f2','#34aa67','#7a4fd7','#ff7a21','#ef4f79'];document.querySelectorAll('#overview .ov-kpi').forEach((card,i)=>{const k=card.dataset.index||['blis','presence','digital','content','market'][i];const a=values(k),mean=avg(a),strong=card.querySelector('.ov-kpi-value strong'),delta=card.querySelector('.ov-delta'),sp=card.querySelector('.ov-spark'),foot=card.querySelector('.ov-kpi-foot');if(strong)strong.textContent=fmt(mean);if(delta){if(a.length<2){delta.textContent='—';delta.className='ov-delta flat'}else{const d=a[a.length-1]-a[0];delta.textContent=`${d>0?'↑':d<0?'↓':'–'} ${Math.abs(d).toFixed(Math.abs(d)%1?1:0)}`;delta.className='ov-delta '+(d<0?'down':d===0?'flat':'')}}if(sp)sp.innerHTML=spark(a,colors[i]||'#2674f2');if(foot)foot.textContent=`средна стойност за ${BLISPeriod.days} дни · промяна начало → край`})}
  function patchTrend(){const host=document.querySelector('#overview .ov-trend');if(!host)return;const a=values('blis');if(a.length<2){host.innerHTML='<div class="ov-no-data">Няма достатъчно BLIS измервания в избрания период.</div>';return}const w=620,h=220,l=34,r=18,t=16,b=30,x=i=>l+i*(w-l-r)/(a.length-1),y=v=>t+(100-v)*(h-t-b)/100,pts=a.map((v,i)=>`${x(i)},${y(v)}`).join(' '),area=`M ${x(0)} ${h-b} L ${a.map((v,i)=>`${x(i)} ${y(v)}`).join(' L ')} L ${x(a.length-1)} ${h-b} Z`;host.innerHTML=`<svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none"><defs><linearGradient id="periodArea" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#2b79f3" stop-opacity=".18"/><stop offset="1" stop-color="#2b79f3" stop-opacity="0"/></linearGradient></defs>${[0,25,50,75,100].map(v=>`<line class="ov-chart-grid" x1="${l}" y1="${y(v)}" x2="${w-r}" y2="${y(v)}"/><text class="ov-chart-label" x="2" y="${y(v)+3}">${v}</text>`).join('')}<path d="${area}" fill="url(#periodArea)"/><polyline points="${pts}" class="ov-chart-line"/>${a.map((v,i)=>`<circle cx="${x(i)}" cy="${y(v)}" r="2.2" fill="#2674f2"/>`).join('')}</svg>`}
  function patchDigital(){const card=document.querySelector('#overview .ov-row-mid > .ov-card:nth-child(2)');if(!card)return;const a=values('digital');const current=a.length?a[a.length-1]:null,first=a.length?a[0]:null,d=current!=null&&first!=null?current-first:null;const cur=card.querySelector('.clarity-metric:nth-child(1) strong'),prev=card.querySelector('.clarity-metric:nth-child(3) strong'),change=card.querySelector('.clarity-change strong'),msg=card.querySelector('.clarity-message');if(cur)cur.innerHTML=`${fmt(current)}<em>/100</em>`;if(prev)prev.innerHTML=`${fmt(first)}<em>/100</em>`;if(change)change.innerHTML=d==null?'—':`${d>0?'+':''}${d.toFixed(1)}<em> пункта</em>`;if(msg)msg.textContent=a.length<2?`Няма достатъчно измервания за последните ${BLISPeriod.days} дни.`:d>0?'Дигиталната видимост се подобрява в избрания период.':d<0?'Дигиталната видимост отслабва в избрания период.':'Няма промяна в избрания период.'}
  function patchTopics(){const card=document.querySelector('#overview .ov-row-mid > .ov-card:nth-child(3)');if(!card||!window.BLISPeriod)return;const arr=BLISPeriod.signals().slice(0,5),body=card.querySelector('.clarity-topics');if(!body)return;if(!arr.length){body.outerHTML=`<div class="clarity-empty">Няма регистрирани сигнали за последните ${BLISPeriod.days} дни.</div>`;return}body.innerHTML=arr.map((x,i)=>`<div class="clarity-topic"><span class="clarity-num">${i+1}</span><div><b>${esc(x.title||x.label||'Наблюдаван сигнал')}</b><small>${esc(x.description||x.detail||'Промяна в наблюдаваната среда')}</small></div>${x.priority?`<span class="clarity-tag">${esc(x.priority)}</span>`:''}</div>`).join('')}
  function patchCompetition(){const card=document.querySelector('#overview .ov-row-mid > .ov-card:nth-child(1)');if(!card||!window.BLISPeriod)return;const arr=BLISPeriod.competitors();const rows=card.querySelector('.clarity-bars');if(!rows||!arr.length)return;const max=Math.max(...arr.map(x=>num(x.score)||0),1);rows.innerHTML=arr.map((x,i)=>`<div class="clarity-bar-row ${i===0?'is-client':''}"><div class="clarity-bar-label"><b>${esc(x.name||'Конкурент')}</b><span>${fmt(x.score)}/100</span></div><div class="clarity-track"><i style="width:${Math.max(4,(num(x.score)||0)/max*100)}%"></i></div></div>`).join('')}
  function run(){if(!window.BLISPeriod)return;patchKpis();patchTrend();patchDigital();patchTopics();patchCompetition();document.querySelectorAll('#overview .ov-pill').forEach(x=>{if(x.textContent.trim()==='История')x.textContent=`${BLISPeriod.days} дни`})}
  function init(){setTimeout(run,300);setInterval(run,1800);window.addEventListener('blis:periodchange',()=>setTimeout(run,50))}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
