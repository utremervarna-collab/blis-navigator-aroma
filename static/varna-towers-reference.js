/* Varna Towers — legacy page override retired.
   Varna Towers now uses the same canonical client-intelligence renderers as every
   other Navigator profile. Sector-specific values come from client data, never
   from a wrapper around window.refGo(). */
(function(){
  'use strict';
  window.__BLIS_VARNA_TOWERS_LEGACY_REFERENCE_RETIRED = true;
})();
