name: Signal Collector Validation
on:
  push:
    branches:
      - milestone/navigator-production-stable-20260825
    paths:
      - signal_collector.go
      - .github/workflows/signal-collector-check.yml
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: go test ./...
