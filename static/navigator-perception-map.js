/* BLIS Navigator — Perception Map runtime temporarily disabled.
   The module will be mounted directly by the core Navigator renderer,
   without route interception, DOM observers or global click hooks. */
(function(){
  'use strict';
  window.BLIS_PERCEPTION_MAP_RUNTIME_DISABLED = true;
})();
