package main

// Legacy Client Intelligence V2 was retired. V3 is the single client-facing
// content owner, so the old compatibility script is no longer injected.
// V3 already suppresses any stale V2 markup left in cached client HTML.
